---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app youtube-dl-server
  namespace: flux-system
spec:
  dependsOn:
    - name: cluster-apps-minio
    - name: volsync
  path: ./kubernetes/main/apps/default/youtube-dl-server/app
  targetNamespace: default
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  postBuild:
    substitute:
      APP: *app
      PVC_NAME_SUFFIX: "-data"
      PVC_STORAGECLASS: longhorn
      PVC_CAPACITY: 1Gi
      VOLSYNC_SNAPSHOTCLASS: longhorn
      VOLSYNC_CACHE_SNAPSHOTCLASS: longhorn-cache
      RESTIC_PASS_KEY: ydl_restic_pass
      MINIO_USER_KEY: ydl_restic_minio_user
      MINIO_PASS_KEY: ydl_restic_minio_pass
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m

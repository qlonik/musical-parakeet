---
name: Reset Actionsflow Cache

on:
  workflow_dispatch:

jobs:
  reset:
    runs-on: ubuntu-latest
    name: Reset Actionsflow Cache
    steps:
      - uses: actions/checkout@4
      - name: Run Actionsflow Clean
        uses: qlonik/actionsflow-action@3
        with:
          args: clean
          json-secrets: ${{ toJSON(secrets) }}

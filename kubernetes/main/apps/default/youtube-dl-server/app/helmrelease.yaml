---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: youtube-dl-server
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.2.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: minio
      namespace: default
  values:
    controllers:
      youtube-dl-server:
        type: statefulset
        statefulset:
          volumeClaimTemplates:
            - name: data
              accessMode: ReadWriteMany
              size: 1Gi
              storageClass: truenas-nfs-main
              globalMounts:
                - path: /youtube-dl/
        containers:
          main:
            image:
              repository: nbr23/youtube-dl-server
              tag: yt-dlp@sha256:c9fc42eeae08770063a260b64e5799c74efb7f6c1247efae993d902ed5f95a21
            env:
              TZ: ${TIMEZONE}
            resources:
              requests:
                cpu: 10m
                memory: 250Mi
              limits:
                memory: 2Gi
        initContainers:
          minio-init-bucket:
            image:
              repository: ghcr.io/qlonik/minio-init-bucket
              tag: RELEASE.2024-06-01T15-03-35Z@sha256:af0f5d613d2c13a963066f56d7f3b5db9a2bb01aabd20210d699c5f2d45b2b25
            envFrom:
              - secretRef:
                  name: youtube-dl-server-secret
    defaultPodOptions:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: "OnRootMismatch"
    service:
      main:
        controller: youtube-dl-server
        ports:
          http:
            port: 8080
    ingress:
      main:
        className: internal
        annotations:
          hajimari.io/icon: mdi:filmstrip
        hosts:
          - host: &host "ytdl.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: http
        tls:
          - hosts:
              - *host
    persistence:
      config:
        type: configMap
        name: youtube-dl-server-config
        globalMounts:
          - path: /app_config/config.yml
            subPath: config.yaml
            readOnly: true
      downloads:
        type: nfs
        server: nova.home.arpa
        path: /mnt/main/media/downloads/youtube/downloads
        globalMounts:
          - path: /youtube-dl/downloads
      playlists:
        type: nfs
        server: nova.home.arpa
        path: /mnt/main/media/downloads/youtube/playlists
        globalMounts:
          - path: /youtube-dl/playlists
      cache-dir:
        type: emptyDir
        globalMounts:
          - path: /youtube-dl/.cache

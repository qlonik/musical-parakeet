---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./externalsecret.yaml
  - ./helmrelease.yaml

configMapGenerator:
  - name: vikunja-config
    files:
      - ./resources/config.yaml
    options:
      disableNameSuffixHash: true

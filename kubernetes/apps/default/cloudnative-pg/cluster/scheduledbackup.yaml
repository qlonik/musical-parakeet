---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres
  namespace: default
spec:
  schedule: "0 */24 * * *"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: postgres

auth:
  local:
    enabled: false
  openid:
    enabled: true
    providers:
      - name: authelia
        authurl: https://auth.${SECRET_DOMAIN}
        clientid: vikunja
        clientsecret: ${SECRET_VIKUNJA_OAUTH_CLIENT_SECRET}

files:
  maxsize: 50M

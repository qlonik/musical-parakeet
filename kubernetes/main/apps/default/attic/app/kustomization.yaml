---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml

configMapGenerator:
  - name: attic-config
    files:
      - ./config/server.toml
    options:
      disableNameSuffixHash: true
